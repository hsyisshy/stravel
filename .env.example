# Vite exposes only VITE_-prefixed vars to the client bundle.
VITE_GOOGLE_MAPS_API_KEY=

# GEMINI_API_KEY is NOT read from here. The Cloud Function reads it from
# Firebase Secret Manager — set it with:
#   firebase functions:secrets:set GEMINI_API_KEY --project <your-project-id>
